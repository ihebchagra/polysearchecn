import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.ihebchagra.polysearchecn',
  appName: 'Polysearch ECN',
  webDir: 'build'
};

export default config;
